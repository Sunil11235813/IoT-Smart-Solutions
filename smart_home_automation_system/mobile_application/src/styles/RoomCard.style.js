import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    backgroundColor: "#fff",
    padding: 10,
    borderRadius: 5,
    margin: 5,
  },
  name: {
    fontSize: 18,
    fontWeight: "bold",
  },
});
export default styles;